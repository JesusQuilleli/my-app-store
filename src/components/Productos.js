import React from "react";
import { View, Text, StyleSheet } from "react-native";

const Productos = () => {
  return (
    <View>
      <Text>Bienvenido Productos</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  
})

export default Productos;
